@model IEnumerable<DCUniverseMVC.Models.Battle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hero.HeroName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Villain.VillainName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hero.HeroName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Villain.VillainName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BattleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BattleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BattleID })
        </td>
    </tr>
}

</table>
