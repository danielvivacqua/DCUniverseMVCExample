@model IEnumerable<DCUniverseMVC.Models.Hero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HeroName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecretIdentity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasCape)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PowerLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HeroName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecretIdentity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasCape)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PowerLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HeroID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HeroID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HeroID })
        </td>
    </tr>
}

</table>
